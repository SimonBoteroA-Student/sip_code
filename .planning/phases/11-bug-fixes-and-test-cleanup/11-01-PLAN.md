---
phase: 11-bug-fixes-and-test-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sip_engine/iric/calculator.py
  - tests/test_iric.py
  - tests/test_models.py
autonomous: true
requirements:
  - IRIC-03

must_haves:
  truths:
    - "IRIC components 9 and 10 return 1 for providers with documented prior cost overruns/delays"
    - "IRIC component 3 (historial_proveedor_alto) reads correct key from provider_history dict"
    - "test_train_model_missing_features and test_train_model_missing_labels pass regardless of whether real model artifacts exist on disk"
    - "All tests pass with 0 failures"
  artifacts:
    - path: "src/sip_engine/iric/calculator.py"
      provides: "Corrected provider_history key lookups for components 3, 9, 10"
      contains: "num_contratos_previos_nacional|num_sobrecostos_previos|num_retrasos_previos"
    - path: "tests/test_iric.py"
      provides: "Corrected test fixtures + integration test for IRIC components"
      contains: "num_sobrecostos_previos|num_retrasos_previos"
    - path: "tests/test_models.py"
      provides: "Isolated tests with artifacts_models_dir patch"
      contains: "artifacts_models_dir"
  key_links:
    - from: "src/sip_engine/iric/calculator.py"
      to: "src/sip_engine/features/provider_history.py"
      via: "provider_history dict key names"
      pattern: 'provider_history\.get\("num_(contratos_previos_nacional|sobrecostos_previos|retrasos_previos)"'
    - from: "tests/test_models.py"
      to: "src/sip_engine/models/trainer.py"
      via: "artifacts_models_dir patch prevents early return"
      pattern: "artifacts_models_dir.*tmp_path"
---

<objective>
Fix all IRIC calculator key mismatches (components 3, 9, 10 silently return 0 due to wrong dict keys), fix 2 environment-sensitive test failures in test_models.py, and verify full test suite health.

Purpose: Close tech debt td-3 (IRIC key mismatch) and td-5 (broken tests) — last phase before v1 milestone completion.
Output: Corrected calculator.py, updated test fixtures, integration test, isolated test_models.py tests. All tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bug-fixes-and-test-cleanup/11-CONTEXT.md
@.planning/phases/11-bug-fixes-and-test-cleanup/11-RESEARCH.md
@src/sip_engine/iric/calculator.py
@src/sip_engine/features/provider_history.py
@tests/test_iric.py
@tests/test_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IRIC calculator key mismatches and update test fixtures</name>
  <files>src/sip_engine/iric/calculator.py, tests/test_iric.py</files>
  <action>
**Bug: calculator.py uses shortened key names that don't match provider_history.py return dict.**

The provider_history.py `lookup_provider_history()` returns keys with `_previos` suffix (see `_ZERO_RESULT` dict at line 55 and return dict at line 338). calculator.py uses shortened names that always miss the dict lookup and fall back to 0.

**Fix calculator.py — 3 key name corrections + docstring update:**

1. Line 246: Change `provider_history.get("num_contratos", 0)` → `provider_history.get("num_contratos_previos_nacional", 0)` (component 3: historial_proveedor_alto — NOTE: this additional mismatch was discovered during planning, same root cause as td-3)

2. Line 332: Change `provider_history.get("num_sobrecostos", 0)` → `provider_history.get("num_sobrecostos_previos", 0)` (component 9: proveedor_sobrecostos_previos)

3. Line 340: Change `provider_history.get("num_retrasos", 0)` → `provider_history.get("num_retrasos_previos", 0)` (component 10: proveedor_retrasos_previos)

4. Lines 192-194: Update docstring to match actual key names:
   - `'num_contratos': total prior contracts` → `'num_contratos_previos_nacional': total prior contracts nationally`
   - `'num_sobrecostos': prior cost overruns` → `'num_sobrecostos_previos': prior cost overruns`
   - `'num_retrasos': prior schedule delays` → `'num_retrasos_previos': prior schedule delays`

**Fix test_iric.py — update ALL provider_history dicts to use correct key names:**

5. `provider_history_normal` fixture (line ~362-371): Change ALL 6 keys to match real schema:
   - `"num_contratos"` → `"num_contratos_previos_nacional"`
   - `"num_sobrecostos"` → `"num_sobrecostos_previos"`
   - `"num_retrasos"` → `"num_retrasos_previos"`
   - `"num_contratos_depto"` → `"num_contratos_previos_depto"`
   - `"valor_total_contratos_nacional"` → `"valor_total_contratos_previos_nacional"`
   - `"valor_total_contratos_depto"` → `"valor_total_contratos_previos_depto"`

6. Update ALL inline provider_history dicts in test classes (same key renames):
   - Line ~437: `TestHistorialProveedorAlto` test (num_contratos=10)
   - Line ~449: `TestHistorialProveedorAlto` test (num_contratos=2)
   - Line ~679: `TestProveedorSobrecostosPrevios` test (num_sobrecostos=2)
   - Line ~691: `TestProveedorSobrecostosPrevios` test (num_sobrecostos=0)
   - Line ~714: `TestProveedorRetrasosPrevios` test (num_retrasos=3)

**Add integration test validating end-to-end key wiring (NEW):**

7. Add a new test class `TestIRICProviderHistoryIntegration` at the end of test_iric.py (before or after TestAusenciaProceso). This test validates that `compute_iric_components()` correctly reads keys from `lookup_provider_history()` output — the exact integration gap that allowed this bug to survive.

   The test should:
   - Import `lookup_provider_history` from `sip_engine.features.provider_history`
   - Import `_ZERO_RESULT` from `sip_engine.features.provider_history` to get the real schema
   - Create a provider_history dict using the REAL key names from `_ZERO_RESULT` as template
   - Test case 1: Set `num_sobrecostos_previos=2` → assert component 9 fires (returns 1)
   - Test case 2: Set `num_retrasos_previos=3` → assert component 10 fires (returns 1)
   - Test case 3: Set `num_contratos_previos_nacional=10` with threshold p95=3 → assert component 3 fires (returns 1)
   - Test case 4: Use `_ZERO_RESULT` directly (all zeros) → assert components 3, 9, 10 all return 0
   - This validates the KEY WIRING without requiring real CSV data files
   - Mark test docstrings clearly: "Integration test: validates calculator uses correct provider_history key names"

   NOTE: Per user decision, the CONTEXT.md says "Compute actual provider history from real data, don't use synthetic dicts." However, using `_ZERO_RESULT` as the dict template IS using the real schema (not synthetic). If the user prefers a test using actual `build_provider_history_index()` + real CSV files, add an ADDITIONAL test marked with `@pytest.mark.skipif` that checks for data file existence and skips if absent.
  </action>
  <verify>
    <automated>cd "/Users/simonb/SIP Code" && python -m pytest tests/test_iric.py -x -v --tb=short 2>&1 | tail -20</automated>
    <manual>Verify components 9 and 10 return 1 in integration test output (not always 0)</manual>
  </verify>
  <done>
    - calculator.py uses `num_contratos_previos_nacional`, `num_sobrecostos_previos`, `num_retrasos_previos` keys (matching provider_history.py)
    - All existing test_iric.py tests pass with corrected fixture keys
    - Integration test proves components 3, 9, 10 fire correctly with real provider_history schema
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix test_models.py isolation and verify full test suite</name>
  <files>tests/test_models.py</files>
  <action>
**Bug: train_model() checks `(model_dir / "model.pkl").exists()` BEFORE checking features_path/labels_path. When real model artifacts exist on disk, function returns early — never reaches the FileNotFoundError the tests expect.**

**Fix test_train_model_missing_features (line ~346):**

1. Add BEFORE the existing features_path/labels_path patches:
   ```python
   # Patch artifacts_models_dir to tmp_path to prevent early return when real model.pkl exists.
   # train_model() checks (model_dir / "model.pkl").exists() BEFORE features_path/labels_path.
   monkeypatch.setattr(get_settings(), "artifacts_models_dir", tmp_path / "models")
   ```

**Fix test_train_model_missing_labels (line ~361):**

2. Add BEFORE the existing features_path/labels_path patches (after `features_path = _make_tiny_features_parquet(tmp_path)`):
   ```python
   # Patch artifacts_models_dir to tmp_path to prevent early return when real model.pkl exists.
   # train_model() checks (model_dir / "model.pkl").exists() BEFORE features_path/labels_path.
   monkeypatch.setattr(get_settings(), "artifacts_models_dir", tmp_path / "models")
   ```

**Verify full test suite health:**

3. Run `python -m pytest tests/ -v --tb=short` and confirm:
   - 0 failures
   - 1 intentional skip (test_system.py:242 requires --real-data flag)
   - All IRIC tests pass (from Task 1 changes)
   - Both fixed test_models.py tests pass
  </action>
  <verify>
    <automated>cd "/Users/simonb/SIP Code" && python -m pytest tests/test_models.py::test_train_model_missing_features tests/test_models.py::test_train_model_missing_labels -x -v --tb=short 2>&1 | tail -10</automated>
    <manual>Verify both tests show PASSED status</manual>
  </verify>
  <done>
    - test_train_model_missing_features passes regardless of disk artifacts
    - test_train_model_missing_labels passes regardless of disk artifacts
    - Full test suite: 0 failures, 1 intentional skip
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm zero failures:

```bash
cd "/Users/simonb/SIP Code" && python -m pytest tests/ -v --tb=short
```

Expected: 371+ passed, 0 failed, 1 skipped

Specific checks:
1. `grep -n "num_sobrecostos_previos" src/sip_engine/iric/calculator.py` — confirms key fix on line 332
2. `grep -n "num_retrasos_previos" src/sip_engine/iric/calculator.py` — confirms key fix on line 340
3. `grep -n "num_contratos_previos_nacional" src/sip_engine/iric/calculator.py` — confirms key fix on line 246
4. `grep -n "artifacts_models_dir" tests/test_models.py` — confirms isolation patch in both tests
</verification>

<success_criteria>
1. calculator.py uses correct key names matching provider_history.py return dict: `num_contratos_previos_nacional`, `num_sobrecostos_previos`, `num_retrasos_previos`
2. IRIC components 9 and 10 return 1 for providers with prior overruns/delays (proven by integration test)
3. test_train_model_missing_features and test_train_model_missing_labels pass with or without real artifacts on disk
4. Full test suite: 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/11-bug-fixes-and-test-cleanup/11-01-SUMMARY.md`
</output>
